<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Soleus</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Quantico:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Quantico', sans-serif;
        }
        .error-message {
            animation: fadeIn 0.3s ease-in;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen bg-black">
    <div class="border-4 border-[#FFC107] p-6 rounded-lg w-full max-w-sm">
        <div class="flex flex-col items-center mb-6">
            <img src="/images/soleus_log.png" alt="Soleus Logo" class="w-20 h-20 mb-2">
            <h2 class="text-xl font-semibold text-[#FFC107] mt-1">ADMIN LOGIN</h2>
        </div>
        <div class="bg-white p-6 rounded-tl-[40px] rounded-br-[40px] shadow-lg">
            <% if (message) { %>
                <div class="mb-4 p-3 bg-red-100 border border-red-400 text-red-700 rounded-lg text-sm error-message">
                    <%= message %>
                </div>
            <% } %>
            <form action="/admin/login" method="POST">
                <div class="mb-4">
                    <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                    <input 
                        type="email" 
                        name="email" 
                        id="email" 
                        placeholder="Enter your email" 
                        class="w-full p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#FFC107] text-sm" 
                        required 
                        aria-required="true"
                    >
                </div>
                <div class="mb-6">
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                    <input 
                        type="password" 
                        name="password" 
                        id="password" 
                        placeholder="Enter your password" 
                        class="w-full p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#FFC107] text-sm" 
                        required 
                        aria-required="true"
                    >
                </div>
                <button 
                    type="submit" 
                    class="w-24 mx-auto block bg-[#4A2C2A] text-white p-2 rounded-lg hover:bg-opacity-90 focus:ring-2 focus:ring-[#FFC107] focus:outline-none transition duration-300 text-sm"
                >
                    Login
                </button>
            </form>
        </div>
    </div>
</body>
</html>