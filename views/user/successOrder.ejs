<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmed - Quantico</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Quantico:wght@400;700&display=swap" rel="stylesheet">
    <style>
        .quantico-light {
            background: linear-gradient(135deg, #ffffff 0%, #f8fafc 50%, #f1f5f9 100%);
        }
        .glass-effect {
            background: rgba(0, 0, 0, 0.03);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(0, 0, 0, 0.08);
        }
        .success-gradient {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        }
        .neon-accent {
            background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
        }
        .luxury-shadow {
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.1), 0 0 0 1px rgba(0, 0, 0, 0.05);
        }
        .success-border {
            border: 1px solid rgba(16, 185, 129, 0.2);
            background: rgba(16, 185, 129, 0.02);
        }
        body {
            font-family: 'Quantico', sans-serif;
        }
        .glow-text {
            text-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .pulse-glow {
            animation: pulse-glow 2s ease-in-out infinite alternate;
        }
        .success-pulse {
            animation: success-pulse 2s ease-in-out infinite alternate;
        }
        .bounce-in {
            animation: bounce-in 0.8s ease-out;
        }
        @keyframes pulse-glow {
            from { box-shadow: 0 0 20px rgba(59, 130, 246, 0.3); }
            to { box-shadow: 0 0 30px rgba(59, 130, 246, 0.6); }
        }
        @keyframes success-pulse {
            from { box-shadow: 0 0 20px rgba(16, 185, 129, 0.4); }
            to { box-shadow: 0 0 40px rgba(16, 185, 129, 0.8); }
        }
        @keyframes bounce-in {
            0% { transform: scale(0.3) rotate(-360deg); opacity: 0; }
            50% { transform: scale(1.1) rotate(-180deg); opacity: 0.8; }
            100% { transform: scale(1) rotate(0deg); opacity: 1; }
        }
        .success-seal {
            width: 120px;
            height: 120px;
            background: radial-gradient(circle, #10b981 0%, #059669 70%, #047857 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            margin: 0 auto;
            box-shadow: 0 0 30px rgba(16, 185, 129, 0.6), inset 0 0 30px rgba(255, 255, 255, 0.1);
        }
        .success-seal::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            border: 3px solid rgba(255, 255, 255, 0.3);
            animation: rotate 3s linear infinite;
        }
        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        /* Static Bubble background design */
        .bubble {
            position: absolute;
            border-radius: 50%;
            filter: blur(1px);
        }
        
        .yellow-bubble {
            background: radial-gradient(circle at 30% 30%, rgba(255, 235, 59, 0.15), rgba(255, 193, 7, 0.08));
            border: 1px solid rgba(255, 235, 59, 0.1);
        }
        
        .black-bubble {
            background: radial-gradient(circle at 30% 30%, rgba(0, 0, 0, 0.12), rgba(0, 0, 0, 0.05));
            border: 1px solid rgba(0, 0, 0, 0.08);
        }
        
        .floating {
            animation: float 6s ease-in-out infinite;
        }
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
    </style>
</head>
<body class="quantico-light min-h-screen text-gray-900 relative overflow-x-hidden">
    <!-- Animated Bubble Background -->
    <div class="fixed inset-0 overflow-hidden">
        <!-- Yellow Bubbles -->
        <div class="bubble yellow-bubble" style="top: 5%; left: 10%; width: 200px; height: 200px;"></div>
        <div class="bubble yellow-bubble" style="top: 30%; left: 70%; width: 150px; height: 150px;"></div>
        <div class="bubble yellow-bubble" style="top: 60%; left: 20%; width: 180px; height: 180px;"></div>
        <div class="bubble yellow-bubble" style="top: 80%; left: 80%; width: 120px; height: 120px;"></div>
        <div class="bubble yellow-bubble" style="top: 15%; left: 50%; width: 100px; height: 100px;"></div>
        
        <!-- Black Bubbles -->
        <div class="bubble black-bubble" style="top: 20%; left: 30%; width: 160px; height: 160px;"></div>
        <div class="bubble black-bubble" style="top: 45%; left: 85%; width: 140px; height: 140px;"></div>
        <div class="bubble black-bubble" style="top: 70%; left: 5%; width: 170px; height: 170px;"></div>
        <div class="bubble black-bubble" style="top: 10%; left: 75%; width: 110px; height: 110px;"></div>
        <div class="bubble black-bubble" style="top: 55%; left: 45%; width: 130px; height: 130px;"></div>
        <div class="bubble black-bubble" style="top: 85%; left: 60%; width: 90px; height: 90px;"></div>
    </div>

    <!-- Main Container -->
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8 lg:py-16">
        
        <!-- Success Section -->
        <div class="text-center mb-12 floating">
            <!-- Success Seal -->
            <div class="success-seal bounce-in success-pulse mb-8">
                <svg class="w-16 h-16 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"></path>
                </svg>
            </div>
            
            <!-- Success Message -->
            <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 glow-text mb-4 uppercase tracking-wider">
                ORDER CONFIRMED
            </h1>
            <p class="text-xl text-green-600 font-bold mb-2 uppercase tracking-wide">Thank you for your purchasing with SOLEUS</p>
            <p class="text-gray-600 text-lg font-medium">Your order has been successfully placed and is being processed.</p>
            <div class="w-32 h-1 success-gradient mx-auto mt-6 rounded-full"></div>
        </div>

        <!-- Order Details -->
        <div class="glass-effect rounded-2xl luxury-shadow p-6 lg:p-8 mb-8">
            <h2 class="text-2xl font-bold text-green-600 mb-6 uppercase tracking-widest flex items-center justify-center">
                <span class="w-2 h-2 bg-green-500 rounded-full mr-3 success-pulse"></span>
                Order Details
            </h2>
            
            <!-- Order Info Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                <div class="success-border rounded-xl p-5">
                    <h3 class="text-green-600 font-bold text-sm uppercase tracking-wider mb-3">Order Information</h3>
                    <div class="space-y-2">
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600 font-medium">Order ID:</span>
                            <span class="text-gray-900 font-bold"><%= orderNumber %></span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600 font-medium">Order Date:</span>
                            <span class="text-gray-900 font-bold"><%= orderData %></span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600 font-medium">Payment Method:</span>
                            <span class="text-blue-600 font-bold"><%= paymentMethod %></span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600 font-medium">Status:</span>
                            <span class="text-green-600 font-bold flex items-center gap-2">
                                <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                                <%= status %>
                            </span>
                        </div>
                    </div>
                </div>
                
                <div class="success-border rounded-xl p-5">
                    <h3 class="text-green-600 font-bold text-sm uppercase tracking-wider mb-3">Delivery Information</h3>
                    <div class="space-y-2">
                        <div class="flex justify-between items-start">
                            <span class="text-gray-600 font-medium">Delivery To:</span>
                            <div class="text-right">
                                <p class="text-gray-900 font-bold"><%= deliveryAddress.name %></p>
                                <p class="text-gray-600 text-sm"><%= deliveryAddress.buildingName  %>, <%= deliveryAddress.landMark %></p>
                                <p class="text-gray-600 text-sm"><%= deliveryAddress.city %>, <%= deliveryAddress.state %> - <%= deliveryAddress.pin %></p>
                            </div>
                        </div>
                        <div class="flex justify-between items-center pt-2">
                            <span class="text-gray-600 font-medium">Expected Delivery:</span>
                            <span class="text-blue-600 font-bold">5-7 WORKING DAYS</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Order Total -->
            <div class="border-t border-green-400/30 pt-6">
                <div class="flex justify-between items-center text-2xl">
                    <span class="text-green-600 font-bold uppercase tracking-wide">AMOUNT:</span>
                    <span class="text-gray-900 font-bold glow-text">₹ <%= priceDetails.total %></span>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <button class="w-full sm:w-auto success-gradient hover:from-green-400 hover:to-emerald-500 text-white font-bold py-4 px-8 rounded-xl transition-all duration-300 text-lg uppercase tracking-wider flex items-center justify-center gap-3 success-pulse">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
                TRACK ORDER
            </button>
            
            <button class="w-full sm:w-auto neon-accent hover:from-blue-400 hover:to-indigo-500 text-white font-bold py-4 px-8 rounded-xl transition-all duration-300 text-lg uppercase tracking-wider flex items-center justify-center gap-3 pulse-glow">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
                </svg>
                CONTINUE SHOPPING
            </button>
        </div>

        <!-- Additional Info -->
        <div class="text-center mt-12">
            <p class="text-gray-500 text-sm font-light tracking-wide mb-2">
                You will receive an email confirmation shortly with your order details.
            </p>
            <p class="text-blue-600 text-sm font-medium">
                Need help? Contact our support team 24/7
            </p>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded',  function() {
            // Add click handlers for buttons
            const trackOrderBtn = document.querySelector('button:has(svg path[d*="9 12l2 2"])');
            const continueShoppingBtn = document.querySelector('button:has(svg path[d*="16 11V7"])');
            
            trackOrderBtn?.addEventListener('click', function() {
                // Add your track order logic here
                console.log('Track Order clicked');
                this.innerHTML = '<svg class="w-6 h-6 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path></svg>TRACKING...';
                
                setTimeout(() => {
                    this.innerHTML = '<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>TRACK ORDER';
                }, 2000);
            });
            
            continueShoppingBtn?.addEventListener('click', function() {
                // Add your continue shopping logic here
                console.log('Continue Shopping clicked');
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 150);
                window.location.replace('/allproducts')
            });
        });
    </script>
</body>
</html>